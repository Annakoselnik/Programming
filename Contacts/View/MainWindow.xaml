<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Contacts" 
        Height="450" 
        Width="650"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="75"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="3"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Height" Value="23"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <!-- растяжение по высоте -->
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <!-- 3/4 высоты -->
            <RowDefinition Height="1*"/>
            <!-- 1/4 высоты -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель: Edit Contact -->
        <GroupBox Grid.Row="0" Grid.Column="0" Header="Edit Contact" Margin="0,0,10,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <!-- заполняет оставшееся пространство -->
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Name:"/>
                <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Text="Phone Number:"/>
                <TextBox Grid.Row="3" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Text="Email:"/>
                <TextBox Grid.Row="5" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

            </Grid>
        </GroupBox>

        <!-- Правая панель: Read Contact -->
        <GroupBox Grid.Row="0" Grid.Column="1" Header="Read Contact" Margin="10,0,0,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>


                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <!-- заполняет оставшееся пространство -->
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Name:"/>
                <TextBlock Grid.Row="1" Text="{Binding Name}" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="2" Text="Phone Number:"/>
                <TextBlock Grid.Row="3" Text="{Binding PhoneNumber}" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="4" Text="Email:"/>
                <TextBlock Grid.Row="5" Text="{Binding Email}" TextWrapping="Wrap"/>

                <TextBlock Grid.Row="6" Text="Contact Type:"/>
                <TextBlock Grid.Row="7" Text="{Binding SelectedContactType.Name}" TextWrapping="Wrap"/>
            </Grid>
        </GroupBox>

        <!-- Кнопки: второй столбец, вторая строка, выровнены по правому краю -->
        <StackPanel Grid.Row="1" Grid.Column="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="10,5,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}"/>
            <Button Content="Load" Command="{Binding LoadCommand}"/>
        </StackPanel>
    </Grid>
</Window>